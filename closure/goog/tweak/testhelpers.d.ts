/**
 * Creates a registry with some entries in it.
 * @param queryParams The query parameter string to use for the registry.
 */
export declare function createRegistryEntries(queryParams: string): void;
