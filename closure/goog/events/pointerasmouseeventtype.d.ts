import { MouseEvents } from './mouseevents.js';
/**
 * An alias for `goog.events.EventType.MOUSE*` event types that is overridden by
 * corresponding `POINTER*` event types.
 */
export declare var PointerAsMouseEventType: MouseEvents;
