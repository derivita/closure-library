import { AbstractTabHandler } from './abstracttabhandler.js';
/**
 * Plugin to handle tab keys when not in lists to add 4 spaces.
 */
export declare class SpacesTabHandler extends AbstractTabHandler {
    /**
     * Plugin to handle tab keys when not in lists to add 4 spaces.
     */
    constructor();
    private noStructuralTyping_closure_goog_editor_plugins_spacestabhandler_SpacesTabHandler;
    getTrogClassId(): string;
    handleTabKey(e: any): boolean;
}
