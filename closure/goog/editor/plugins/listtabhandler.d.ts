import { AbstractTabHandler } from './abstracttabhandler.js';
/**
 * Plugin to handle tab keys in lists to indent and outdent.
 */
export declare class ListTabHandler extends AbstractTabHandler {
    /**
     * Plugin to handle tab keys in lists to indent and outdent.
     */
    constructor();
    private noStructuralTyping_closure_goog_editor_plugins_listtabhandler_ListTabHandler;
    getTrogClassId(): string;
    handleTabKey(e: any): boolean;
}
